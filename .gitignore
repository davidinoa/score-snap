node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Evaluation corpus — real scoresheets with real names/dates.
# Kept out of the repo so open-sourcing later is frictionless (PRD §9.1).
fixtures/
